library(tailfindr)

print('running')
find_tails(fast5_dir = '/scratch/tcastigl/ONT_liver',
             save_dir = '/scratch/tcastigl/data/tail_findr_liver_res',
             csv_filename = 'out_liver',
             basecall_group = 'Basecall_1D_001',
             num_cores = 14,
          )
print('done')